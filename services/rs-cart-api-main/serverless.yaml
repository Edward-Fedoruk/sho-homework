service: rs-cart-api-main

plugins:
  - serverless-offline

provider:
  name: aws
  runtime: nodejs14.x
  profile: Admin
  
functions:
  main:
    handler: dist/main.handler
    events:
      - http:
          method: ANY
          path: /
      - http:
          method: ANY
          path: '{proxy+}'